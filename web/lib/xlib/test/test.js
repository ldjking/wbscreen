// JavaScript Document
var total,success,fail;
function test(pre,exp1,exp2,equal){
	if(equal)	return equal(exp1,exp2);
	
	
}

/*pre 准备条件  exp1 表达式一  可以是函数  exp2 表达式二   equal函数  一定要好用才行啊，第几项失败*/

module.exports={
		
}